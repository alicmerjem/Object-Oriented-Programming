package org.example;
import java.util.*;

public class Task5 {
    public ArrayList<Integer> generateNumbers(int n) {
        ArrayList<Integer> result = new ArrayList<>();

        for (int i = 0; i <=n; i++) {
            result.add(i);
        }

        return result;
    }
}
